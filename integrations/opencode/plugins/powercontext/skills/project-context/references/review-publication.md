# Review and publication

Use `pc_review_list` and `pc_review_get` for requested candidate inspection. Use `pc_experience_get` or `pc_skill_get`
with exact returned artifact references. Candidate decision mutations are not model tools in this host. Generation,
inspection, and suggested edits never grant approval, publication, installation, or execution authority.
