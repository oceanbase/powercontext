interface:
  display_name: "Project Context"
  short_description: "Restore and maintain PowerContext project memory."
  default_prompt: "Use PowerContext to restore project context or prepare a handoff."
