{
  "powercontext": {
    "type": "http",
    "url": "${user_config.server_url}/mcp",
    "headersHelper": "python3 -c 'import json, os; value = os.environ.get(\"POWERCONTEXT_CLAUDE_AUTHORIZATION\"); print(json.dumps({\"Authorization\": value} if value else {}))'"
  }
}
